%h1 Payment Info Confirmation
%p Your payment information was successfully saved.

%p.header First Name
%p.form= @result.customer.first_name
%p.header Last Name
%p.form= @result.customer.last_name
%p.header Phone
%p.form= @result.customer.phone
%p.header Credit Card
- @result.customer.credit_cards.each do |cc|
  %p.form= OtherUtil.only_last(cc.masked_number)

- if @offer
  = link_to "Continue with purchase", new_transaction_path(@offer), {class: 'special'}