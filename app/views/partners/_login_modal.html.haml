.modal#partnerLogin(data-form="fancyform_login")
  .header 
    .green
      Partner Log In
  .body
    = form_for(partner,:method => 'post', :remote => true, :url => session_path(:partner, :fancylogin => true)) do |f|
      .errors
      = f.label :email
      = f.email_field :email, :autocomplete=>"off"
      = f.label :password
      = f.password_field :password, :autocomplete=>"off"
      .inline
        = f.check_box :remember_me
        = f.label :remember_me
      .clean
      = f.submit "Sign in", :class => 'button'
  .footer
    = link_to 'Forgot your password?', new_partner_password_path, :target => "_top"

:css
  .button
  {
    border-radius: 22px;
    border: 0
  }
